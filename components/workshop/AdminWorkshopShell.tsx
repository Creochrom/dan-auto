"use client";

import { WorkshopCommandPalette } from "@/components/workshop/WorkshopCommandPalette";

/** Global workshop UX — command palette on all admin routes. */
export function AdminWorkshopShell() {
  return <WorkshopCommandPalette />;
}
